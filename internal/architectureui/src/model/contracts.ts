export type Location = { file: string; line: number; column: number }
export type MemberRecord = { id: string; kind: string; name: string; display_signature: string; accessibility: string; type?: string; is_static?: boolean; is_abstract?: boolean; is_read_only?: boolean; property_get_accessibility?: string; property_set_accessibility?: string; locations?: Location[] }
export type TypeRecord = { id: string; name: string; qualifiedName: string; namespace: string; project: string; projectPath?: string; kind: string; accessibility?: string; modifiers: string[]; memberAvailability: 'available' | 'unavailable' | 'unknown'; members: MemberRecord[]; locations: Location[]; raw: Record<string, unknown> }
export type DependencyRecord = { id: string; source: string; destination: string; kind: string; locations: Location[]; evidence: Array<{ location: Location; originating_member_id?: string; referenced_symbol?: string }>; raw: Record<string, unknown> }
export type AnalysisRecord = { extensionVersion?: string; identityVersion?: string; extractionMode: string; analyzerVersion?: string; completeness: 'complete-within-scope' | 'partial' | 'unknown'; omissions: number; diagnostics: unknown[]; raw: Record<string, unknown> }
export type NormalizedGraph = { types: TypeRecord[]; dependencies: DependencyRecord[]; projects: Array<{ id: string; name: string; path?: string; typeIds: string[] }>; analysis: AnalysisRecord; raw: Record<string, unknown> }
